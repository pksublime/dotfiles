#################################
# Linux-specific ZSH configuration
#################################

if [[ -n "$VSCODE_IPC_HOOK_CLI" ]]; then
    export GIT_EDITOR="code --wait"
else
    export GIT_EDITOR="vim"
fi

# Buildroot SDK
export PATH="$PATH:/opt/x86_64-buildroot-linux-gnu_sdk-buildroot/bin"
export PATH="$PATH:/opt/aarch64-buildroot-linux-gnu_sdk-buildroot/bin"
export BR2_DL_DIR="/var/cache/buildroot-dl"

export SSH_AUTH_SOCK="$HOME/.ssh/agent.sock"
